// import {Mat, noArray} from 'mirada'
// import { write } from 'fs'

// interface Options {
//   prev:Mat[]
//   current:Mat
//   timeMask:Mat
// }

// async function f(o:Options){
//   const hist = new cv.Mat()
//   cv.calcHist(o.timeMask, 1, noArray(), hist, 32,new cv.Size(1, o.timeMask.cols), ) 
//   write
// }